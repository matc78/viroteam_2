{
  "name": "viroteam-functions",
  "description": "Cloud Functions ViroTeam — invitations, HelloAsso checkout/webhook, reçus PDF",
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && tsc -p tsconfig.test.json && node --test lib/*.test.js",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0",
    "pdfkit": "^0.15.2"
  },
  "devDependencies": {
    "@types/pdfkit": "^0.13.9",
    "typescript": "^5.6.0"
  },
  "private": true
}
