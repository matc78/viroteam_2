import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Dernières actions — ViroTeam",
  description: "Journal des actions récentes du club.",
};

/**
 * Route Activité — contenu rendu par DashboardModulePanels (keep-alive).
 */
export default function ActivityPage() {
  return null;
}
