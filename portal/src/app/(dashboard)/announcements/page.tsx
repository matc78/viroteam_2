import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Annonces — ViroTeam",
  description: "Publier et suivre les annonces du club.",
};

/**
 * Route Annonces — contenu rendu par DashboardModulePanels (keep-alive).
 */
export default function AnnouncementsPage() {
  return null;
}
