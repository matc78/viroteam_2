import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Équipements — ViroTeam",
  description: "Inventaire simple du matériel du club.",
};

/**
 * Route Équipements — contenu rendu par DashboardModulePanels (keep-alive).
 */
export default function EquipmentPage() {
  return null;
}
