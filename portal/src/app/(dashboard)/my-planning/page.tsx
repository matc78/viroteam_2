import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Mon planning — ViroTeam",
  description: "Agenda personnel multi-clubs — toi, tes équipes et tes enfants.",
};

/**
 * Route Mon planning — contenu rendu par DashboardModulePanels (keep-alive).
 */
export default function MyPlanningPage() {
  return null;
}
