import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Équipe — ViroTeam",
  description: "Votre équipe et vos coéquipiers.",
};

/** Route Équipe — contenu rendu par DashboardModulePanels (keep-alive). */
export default function TeamPage() {
  return null;
}
