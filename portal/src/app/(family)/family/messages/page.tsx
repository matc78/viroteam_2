import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Messagerie — ViroTeam",
  description: "Discussions multi-clubs — équipes, parents, coaches.",
};

/**
 * Route Messagerie famille — contenu rendu par FamilyModulePanels (keep-alive).
 */
export default function FamilyMessagesPage() {
  return null;
}
