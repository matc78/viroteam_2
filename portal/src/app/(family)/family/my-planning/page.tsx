import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Mon planning — ViroTeam",
  description: "Agenda personnel multi-clubs — toi, tes équipes et tes enfants.",
};

/**
 * Route Mon planning (espace famille) — contenu FamilyModulePanels.
 */
export default function FamilyMyPlanningPage() {
  return null;
}
