import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Paramètres famille — ViroTeam",
  description: "Compte et sécurité de ton espace famille.",
};

/**
 * Route Paramètres famille — contenu rendu par FamilyModulePanels (keep-alive).
 */
export default function FamilySettingsPage() {
  return null;
}
