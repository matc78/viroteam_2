import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Équipe — Espace famille — ViroTeam",
  description: "Équipe de votre enfant ou la vôtre.",
};

/** Route Équipe famille — contenu rendu par FamilyModulePanels. */
export default function FamilyTeamPage() {
  return null;
}
